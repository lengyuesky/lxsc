# 听歌统计验收

2026-09-08，新增控制台 `#listening` 页面、网页实听上报、Subsonic 客户端估算及 SQLite 统计存储。只从新版首次打开数据库时启用统计，旧播放历史保留且不回填。

## 验证结果

- `go test ./...`、`go test -race ./...`、`go vet ./...` 通过；最终后端调整后，数据库、用户接口、Subsonic 的竞态测试再次通过。
- `node --test tests/*.test.mjs tests/web/*.test.cjs`：36 项全部通过，其中新增 9 项计时行为测试。
- `node scripts/licenses.mjs --check` 与 `git diff --check` 通过；没有新增运行依赖。
- `tests/web/browser.cjs`：29 项真实浏览器验收全部通过，其中听歌统计新增 10 项。使用临时 SQLite、本地合成 WAV 与隔离测试账号，不读取生产数据库或请求外部音乐平台。

计时验证包括暂停、缓冲、拖动、倍速、跨北京时间午夜、后台长采样间隔、失败重试、乱序累计值、并发去重、快速切歌的迟到事件，以及退出换号。HTTP 页面缺少 `crypto.randomUUID` 的兼容路径也有独立测试。

数据库验证包括旧库升级不回填、元数据独立快照、SQLite 快照恢复与重启保留、删除用户级联清理。权限验证包括未登录拒绝访问、个人查询隔离、禁止普通用户查看全站与其他用户，以及进度请求绑定原账号。

## 视觉与交互

- 桌面 1440×1080、平板 820×1180、手机 390×844 均验证浅色和深色主题，无页面横向溢出。
- 验证空状态、失败重试、管理员用户排行与筛选、7／30／90／365 天切换、滑块与日历方向键操作。
- 手机端实际播放时，底部播放器不会遮挡统计说明。
- 7／30 天使用带日期的月历布局，90／365 天使用可横向滚动的热力日历。
- 布局截图使用明确的测试示例数据；真实 WAV 计时及客户端去重另外通过实际接口验证。

首次交付的截图与完整浏览器结果位于 `/tmp/lxsc-browser-results-nyKQxc/`，包含 `results.json`、空状态和六种布局截图。可按 README 的 Playwright 命令重新生成。

## 日期查看交互改进（2026-09-08）

- 移除趋势图和日历的悬停选中行为，获得焦点也不改选；点击、触屏点按、回车／空格确认或方向键导航才切换日期。
- 增加带范围限制的原生日期选择框、前后一天按钮和「回到今天」，保留逐日滑块并显示起止日期。边界按钮自动禁用，空值和越界输入提示原因且保留上次结果。
- 详情补充年份、星期、当天合计时长；趋势图用竖向标线及实听／估算两个圆点同步标出选中日期。
- 刷新保留选中日期、键盘焦点和日历横向滚动位置。日期框编辑或滑块拖动期间暂缓静默刷新，已经发出的迟到响应也不会重建控件；日期因跨日更新移出统计范围时回到今天。

本轮先通过新增用例复现「鼠标移过改选日期」，修复后完成以下验证：

- `node --test tests/*.test.mjs tests/web/*.test.cjs`：41 项通过，其中 `tests/web/listening-ui.test.cjs` 新增 5 项交互回归测试。
- `tests/web/browser.cjs`：35 项 Chromium 验收通过；覆盖悬停／点击区分、日期输入及越界、跨年星期、非北京时间浏览器、键盘与触屏、刷新保留状态及刷新途中开始编辑的竞态。
- 桌面 1440×1080、平板 820×1180、手机 390×844、窄屏 320×740 均验证浅色与深色主题，日期控件不超出卡片，无页面横向溢出。
- `go test ./...`、`go test -race ./...`、`go vet ./...`、`git diff --check` 通过；没有新增运行依赖或修改后端统计口径。

本轮完整结果及八种布局／趋势卡片截图位于 `/tmp/lxsc-browser-results-00YTPS/`。仅使用隔离测试服务、临时数据库和示例数据，未部署或重启运行服务。

## 使用边界

客户端只提供歌曲播放上报，无法据此还原实际收听秒数，因此独立标注为估算；缺失歌曲时长时仅计次数。网页每 15 秒上报并在暂停、切歌和离开页面时补报，短暂网络失败在当前页面重试；浏览器强制关闭或离线退出时未送达的进度可能丢失。

以上为提交前的代码与隔离环境验收记录。静态资源嵌入二进制，需要重新构建和部署后才能在运行服务中看到新入口；部署结果应以对应提交、镜像版本和运行服务的实际核验为准。
